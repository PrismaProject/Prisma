package me.Prisma.Main;

import me.Prisma.HWID.WebUtils;
import scala.xml.Null;

import javax.swing.*;
import java.io.IOException;

public class FakeSenseMain {

    public static void Cracked() throws IOException {
        final String version1 = null;
        if (WbxMain.version.equals(version1)) {
        } else
            JOptionPane.showMessageDialog(null, "您的身份证明已经过期了，请更新");
        System.exit(0);
        if (version1 == null) {
        }else{
            if (WebUtils.get("https://gitee.com/NatsuCN/prisma-sense/raw/master/Version").contains(version1)){
            }
        }
    }
}